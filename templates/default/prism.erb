#!/bin/sh
# Auto-generated by Chef for <%= node.name %>

if [ -z "$PRISM_JAVA_HOME" ] ; then
  export PRISM_JAVA_HOME=<%=@prism_path%>/jre
fi
  export LD_LIBRARY_PATH=<%=@prism_path%>/bin:<%=@prism_path%>/lib:/opt/Loquendo/LTTS7/bin<%=":" + @ld_library_extras.join(":") unless @ld_library_extras.empty?%>
  export LTTS7_DEFAULTSESSION=/opt/Loquendo/LTTS7/bin/default.session
 <%=@prism_path%>/lib/vxlaunch "$@" --product-name Prism --config-dir <%=@prism_path%>/conf/ --log-dir <%=@prism_path%>/logs/
